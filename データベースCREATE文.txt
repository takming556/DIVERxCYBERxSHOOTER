CREATE DATABASE DCS;
CREATE TABLE ranking(
	id INT AUTO_INCREMENT,
	nickname VARCHAR(32) COLLATE utf8mb4_unicode_bin NOT NULL,
	score INT UNSIGNED NOT NULL,
	date TIMESTAMP NOT NULL,
	sent_from VARCHAR(32),
	PRIMARY KEY (id)
);